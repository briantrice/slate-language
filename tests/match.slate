
assert: (Nil matches: Nil).

assert: (Nil matches: 4).

assert: (lobby matches: Nil).

assert: ({} matches: {}).

assert: ({1. 2. 3} matches: {1. 2. 3}).

assert: ({Nil. 2} matches: {2. Nil}).

assert: ({1. 2. {3. 4}} matches: {Nil. 2. {3. Nil}}).
